import React from 'react';
import styled from 'styled-components';
import ShareCard from '../common/ShareCard';

const CollectedShares = () => {
  return <ShareCard />;
};

export default CollectedShares;
